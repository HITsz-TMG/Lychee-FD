x-app-build: &app-build
  context: .
  dockerfile: docker/Dockerfile
  args:
    BASE_IMAGE: "${LYCHEE_FD_BASE_IMAGE:-lychee-fd-early-exit-backend:dev}"

services:
  token2wav:
    build: *app-build

  frontend:
    build: *app-build

  backend:
    build: *app-build
