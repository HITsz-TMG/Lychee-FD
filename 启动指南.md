# 启动指南

本项目默认不携带模型权重。启动前请准备以下路径，并按需替换环境变量。

Required local assets:

- `STEPAUDIO_MODEL_PATH`: Lychee-FD full-duplex checkpoint.
- `STEPAUDIO_T2W_MODEL_PATH`: Step-Audio-2-mini `token2wav` directory.
- `STEPAUDIO_CLONE_PROMPT_DIR`: prompt voice directory, default is `frontend/public/clone_24k_mono`.
- `ALLOWED_MODEL_ROOT`: frontend controller model whitelist root, default is current repo.

## 0. Docker 快速启动

推荐使用预构建镜像启动，避免本地重复构建 CUDA / conda / vLLM / flash-attn 环境。

```text
ghcr.io/your-github-username/lychee-fd:latest
```

镜像只包含运行环境和代码，不包含模型权重。模型权重需要下载到宿主机，并通过 volume 挂载进容器。

### 0.1 用户启动步骤

准备配置：

```bash
cp .env.docker.example .env
```

然后修改 `.env`：

```dotenv
HOST_MODEL_ROOT=/path/to/model-root
STEPAUDIO_MODEL_PATH=/data/zxy/model/path/to/lychee-fd/checkpoint
STEPAUDIO_T2W_MODEL_PATH=/data/zxy/model/token2wav
```

注意：`HOST_MODEL_ROOT` 是宿主机真实路径；容器内统一挂载为 `/data/zxy/model`。

拉取镜像并启动：

```bash
docker compose pull
docker compose up
```

打开：

```text
http://127.0.0.1:8084
```

如果要使用本机私有 preset 文件，可以在 `.env` 设置：

```dotenv
PRESETS_FILE_HOST=./model_presets.local.json
```

### 0.2 维护者发布镜像到 GHCR

先在 GitHub 创建 Personal Access Token：

1. 打开 GitHub Settings -> Developer settings -> Personal access tokens。
2. 创建 token，勾选 `write:packages`、`read:packages`。
3. 保存 token，后面登录 GHCR 会用到。

本机已经构建好镜像后，给镜像打 tag。下面命令里的用户名和仓库名必须全部小写：

```bash
docker tag lychee-fd:dev ghcr.io/<github-username>/lychee-fd:latest
docker tag lychee-fd:dev ghcr.io/<github-username>/lychee-fd:v0.1.0
```

登录 GHCR：

```bash
docker login ghcr.io -u <github-username>
```

密码位置粘贴刚创建的 GitHub token。

推送镜像：

```bash
docker push ghcr.io/<github-username>/lychee-fd:latest
docker push ghcr.io/<github-username>/lychee-fd:v0.1.0
```

推送后，把 `.env.docker.example` 和 `compose.yaml` 中的：

```text
ghcr.io/your-github-username/lychee-fd:latest
```

替换成真实镜像地址。

如果 GHCR package 默认是 private，需要在 GitHub 仓库或 Packages 页面把 package visibility 改成 public。

### 0.3 Google Drive 备用镜像包

如果用户无法稳定访问 GHCR，可以提供离线镜像包作为备用下载：

```bash
docker save ghcr.io/<github-username>/lychee-fd:v0.1.0 | gzip > lychee-fd-v0.1.0.tar.gz
```

上传 `lychee-fd-v0.1.0.tar.gz` 到 Google Drive。

用户下载后加载：

```bash
gunzip -c lychee-fd-v0.1.0.tar.gz | docker load
```

然后在 `.env` 中设置：

```dotenv
LYCHEE_FD_IMAGE=ghcr.io/<github-username>/lychee-fd:v0.1.0
```

再启动：

```bash
docker compose up
```

### 0.4 本地重新构建镜像

如果需要从源码重新构建 app 镜像，使用额外的 build compose 文件：

```bash
docker compose -f compose.yaml -f compose.build.yaml build frontend
```

本项目的 `docker/Dockerfile` 默认基于：

```text
lychee-fd-early-exit-backend:dev
```

如果你已经把基础运行环境也发布到了镜像仓库，可以在 `.env` 修改：

```dotenv
LYCHEE_FD_BASE_IMAGE=ghcr.io/<github-username>/lychee-fd-base:latest
```

## 1. Token2Wav 服务

```bash
CUDA_VISIBLE_DEVICES=0 \
PYTHON_BIN=/path/to/python \
STEPAUDIO_T2W_MODEL_PATH=/path/to/Step-Audio-2-mini/token2wav \
./scripts/start_token2wav_server.sh
```

Health check:

```bash
curl -s -X POST http://127.0.0.1:8091/v1/token2wav/health; echo
```

## 2. 前端与实时后端 Controller

```bash
cd /path/to/Lychee-FD
CUDA_VISIBLE_DEVICES=1 \
STEPAUDIO_CONDA_ENV_PATH=/path/to/conda/env \
ALLOWED_MODEL_ROOT=/path/to/model/root \
AUTO_LOAD_DEFAULT=0 \
STEPAUDIO_REALTIME_STRICT_INFER_WINDOW=1 \
STEPAUDIO_STOKEN_DELAY_NUM=10 \
STEPAUDIO_TTS_VOCODER_HOP_SIZE=10 \
STEPAUDIO_T2W_STREAM_LOOKAHEAD_LEN=3 \
STEPAUDIO_T2W_REMOTE_ENABLED=1 \
STEPAUDIO_T2W_REMOTE_URL=http://127.0.0.1:8091 \
STEPAUDIO_T2W_REMOTE_FALLBACK=0 \
STEPAUDIO_USE_VLLM=1 \
STEPAUDIO_VLLM_MAX_MODEL_LEN=16384 \
STEPAUDIO_VLLM_TAIL_TRUNCATE_ENABLED=0 \
STEPAUDIO_SOULX_ENABLED=0 \
STEPAUDIO_SOULX_IDLE_COMPACT_ENABLED=0 \
STEPAUDIO_REALTIME_DEBUG=0 \
VUE_APP_REALTIME_DEBUG=0 \
./scripts/start_frontend_dev.sh prod public
```

打开 `http://127.0.0.1:8084`。如果通过页面选择 preset，请先把 `model_presets_dev.json` 中的示例路径替换成本机 checkpoint。

## 3. HF 后端

将 `STEPAUDIO_USE_VLLM=1` 改为 `STEPAUDIO_USE_VLLM=0`，或在 controller 中选择 `hf` backend。

```bash
CUDA_VISIBLE_DEVICES=1 \
STEPAUDIO_MODEL_PATH=/path/to/lychee-fd/checkpoint \
STEPAUDIO_USE_VLLM=0 \
STEPAUDIO_T2W_REMOTE_ENABLED=1 \
STEPAUDIO_T2W_REMOTE_URL=http://127.0.0.1:8091 \
./scripts/start_backend.sh stable hf
```

## 4. 可选：用户音色上传

```bash
USER_VOICE_ASR_MODEL_DIR=/path/to/paraformer-zh \
./scripts/start_user_voice_upload.sh
```

服务默认监听 `0.0.0.0:18092`。

## 5. 清理进程

```bash
pkill -f "lychee_fd.token2wav_server"
pkill -f "lychee_fd.controller"
pkill -f "lychee_fd.app"
pkill -f "lychee_fd.user_voice_server"
```
